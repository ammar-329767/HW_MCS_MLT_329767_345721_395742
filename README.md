# HW_MCS_MLT_329767_345721_395742
SVU/MLT Assignment

How to run the code via VS Code

First, open the project folder in VS Code, then follow the steps below:
1. Install Python version 3.10
   
3. Install the libraries: open a new terminal and run the following command:
pip install -r requirements.txt

5. Train the model: in the terminal, run the following command:
python train_model.py

7. Run the application: in the terminal, run the following command:
python app.py
